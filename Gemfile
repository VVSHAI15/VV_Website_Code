source "https://rubygems.org"

# Matches what GitHub Pages runs, so a local preview builds the same site.
# Local preview:  bundle install  &&  bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
